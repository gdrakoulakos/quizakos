"use client";
import { Commissioner } from "next/font/google";

export const commissioner = Commissioner({
  subsets: ["latin", "greek"],
  weight: ["600"],
  style: ["normal"],
  display: "swap",
});
